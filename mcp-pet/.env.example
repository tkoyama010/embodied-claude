# === Vision: USB Webcam ===
PET_VISION_USB=true
PET_VISION_USB_INDEX=0

# === Vision: ONVIF PTZ Camera (optional) ===
# Set PET_VISION_ONVIF_HOST to enable PTZ camera
# PET_VISION_ONVIF_HOST=192.168.1.100
# PET_VISION_ONVIF_USERNAME=your-name
# PET_VISION_ONVIF_PASSWORD=your-password
# PET_VISION_ONVIF_PORT=2020
# PET_VISION_ONVIF_MOUNT=normal    # normal | ceiling
# PET_VISION_ONVIF_STREAM_URL=     # Custom RTSP URL (optional)

# === Vision: SkyWay Remote Camera (reads latest frame from disk) ===
# Set to the path where embodied handler saves latest.jpg
# PET_VISION_SKYWAY_FRAME=/path/to/workingDirs/embodied/frames/latest.jpg

# === Built-in Web Server (optional) ===
# Set PET_SERVER_PORT to enable the built-in web server.
# When enabled, mcp-pet serves client/viewer HTML and handles WebSocket frame relay.
# SkyWay frame path is auto-configured (no need to set PET_VISION_SKYWAY_FRAME).
# PET_SERVER_PORT=3000
# PET_SERVER_HOST=0.0.0.0
# PET_SKYWAY_KEY=your-skyway-api-key
# PET_SKYWAY_ROOM=mcp-pet
# PET_SAVE_INTERVAL=10

# === Capture Settings ===
# PET_CAPTURE_DIR=/tmp/mcp-pet
# PET_CAPTURE_MAX_WIDTH=1920
# PET_CAPTURE_MAX_HEIGHT=1080
